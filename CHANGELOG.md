# ЖУРНАЛ ИЗМЕНЕНИЙ

> Как заполнять: все новые правки сначала пиши в "В разработке", а при релизе переноси в блок версии.

## [В разработке]

### 09-05-2026 — план (День 3: враги / застревания) и документация

#### Добавлено
- `Kitchen|Metrics` на `AKitchenGenerator`: счётчики топологии, пикапов, портала, томатов; строка `KitchenGen:` на экране (dev); синхронизация `tomFail` из `SpawnTomatoesIfConfigured`.
- `ChaseRepathCooldownStuckSeconds` в `TomatoSaurusAIController`: укороченный интервал перепути при застревании (BUG-011).
- Документы для куратора в колледже: `Docs/CURATOR_COLLEGE_PRESENTATION.md`, `Docs/INSTALLER_BUILD.md`; обновлены `PORTFOLIO_NOTES.md`, `README_DEMO.txt`, `Tools/InnoSetup_MyProject_Demo.iss`, ссылка в `DIPLOMA_DEMO_RUNBOOK.md`.
- Готовая презентация PowerPoint: `Docs/CURATOR_COLLEGE_PRESENTATION.pptx` (генератор `Tools/gen_curator_presentation_pptx.py`, формат как в «Восстановление Курсора»).
- Установщик: скрипт `Tools/BuildInstaller.ps1`, обновлён `Tools/InnoSetup_MyProject_Demo.iss` (выход в `Dist/`, README в состав установки, путь к билду через `/D` или `installer_game_source_path.txt`), пример пути `Tools/installer_game_source_path.example.txt`, в `.gitignore` добавлены `Dist/` и локальный `installer_game_source_path.txt`. Исправлено: по умолчанию в Inno GUI копируется `..\Build\Packaged\WindowsNoEditor\*` (не фиктивный `D:\Path\To\...`); версия установщика **0.2.2** (`UsePreviousAppDir=no`, установка в `%LocalAppData%\Programs\HonorKitchenDemo\v<версия>`, ярлыки с версией); в игру кладётся `BUILD_FROM_INSTALLER.txt` со штампом пути и даты exe; перед ISCC скрипт печатает жёлтый блок проверки источника.

#### Изменено
- AI (`TomatoSaurusAIController`): при истечении `TargetMemoryUntil`, если на цель есть прямой LOS — память продлевается (нет ложного Investigate при видимом игроке); шум/крошка не прерывает Chase и Melee при валидной цели; перепуть чаще при серии провалов MoveTo или отсутствии прогресса; `MoveToActor` в Chase/retry с `bStopOnOverlap=false` для лучшего скольжения вдоль геометрии.
- AI (`TomatoSaurusAIController`): добавлен единый locomotion arbiter (`Nav` vs `Direct`) и общий watchdog прогресса движения; recovery теперь считается по `RecoveryAttempts` и переключает контролируемо в `InvestigateLastSeen` при исчерпании попыток. Убран одновременный запуск nav/direct в recovery.
- `BUGS.md`, `ROADMAP_14D.md`, `Docs/ROADMAP_14D_CHECKLIST.md`, `Docs/REGRESSION_CHECKLIST.md`: актуализация статусов и прогресса по плану.

#### Исправлено
- BUG-015 (дребезг Chase/Investigate при крошке и долгой погоне с LOS): закрыт в dev; регресс — см. чек-лист §4.

#### Итог дня / не закрыто
- На конец 09-05-2026 BUG-019 остаётся открытым: в `Chase` у части врагов сохраняется замирание; рабочий `Melee` и корректный `Investigate` подтверждены прогоном.
- Этап по плану не менялся: День 3 остаётся активным до выполнения пункта «10 запусков + частота».

### Накопленное ранее (до группировки по датам)

#### Добавлено
- Русифицированы шаблоны рабочих документов. Принят и зафиксирован 14-дневный чек-лист.
- Тестовая выдача набора предметов по клавише `=` (по 3 шт.: аптечки, батарейки, вода, крошки, соль).

#### Изменено
- Инвентарь поддерживает стаки одинаковых предметов до 3 в одном слоте.
- Зафиксирован блокер сборки BUG-004 (C3859/C1076, код 1455 — недостаточный файл подкачки).
- Для BUG-004 подтверждён временный обходной путь: сборка проходит при повышенном приоритете Visual Studio.
- Тестовая выдача по `=` переведена в режим рефилла до 3 для каждого типа без раздувания хотбара.
- Добавлено переключение активного слота хотбара колесом мыши.
- Добавлена точечная выдача по `NumPad`: `1` аптечка, `2` батарейка, `3` вода, `4` крошка, `5` соль (каждый до 3 шт.).
- Добавлена экранная подсказка тест-клавиш при старте локальной сессии.
- Убраны лишние отладочные выводы из игры: `Room Doors`, `Tomato visible`, строка `Крошки X/Y` в HUD.
- Упрощён fallback-детект игрока: приоритет отдан прямой линии видимости (без углового фильтра fallback).
- Для стабильности AI: анти-дребезг переходов (`MinStateHoldTime`, `TransitionDebounceTime`, `NoiseCooldownPerSource`); анти-флап при провале `MoveToActor` (direct / InvestigateLastSeen по дистанции).
- Убран ранний return в Tick во время Investigate, дававший замирание.

#### Исправлено
- Частично стабилизирован детект врагов вблизи и при прямой видимости (BUG-012 — в работе).
- Тестовая выдача батареек: `=` и `NumPad2` засчитывают батарейки в прогресс портала (BUG-016).
- BUG-017: Investigate снова двигается к шуму.
- BUG-018: убран ложный краткий Melee при потере цели за стеной.

#### Удалено
- (пока нет)

---

## [v0.1-demo] - 09-05-2026

### Добавлено
- Базовый игровой цикл для показа куратору.

### Изменено
- …

### Исправлено
- …

### Примечания
- Версия, показанная на защите/куратору.
